{
  "$schema_comment": "Hooks que automatizan la verificación. El harness los ejecuta, no el agente, así que no se pueden saltar.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -m unittest discover -s tests -q 2>&1 | tail -3",
            "description": "Tras editar o escribir archivos, corre los tests y muestra el resumen."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "./init.sh > /tmp/harness_init.log 2>&1 && echo '[harness] init.sh OK' || (echo '[harness] init.sh FALLÓ — revisa /tmp/harness_init.log antes de cerrar' && cat /tmp/harness_init.log | tail -20)",
            "description": "Antes de cerrar la sesión, fuerza una verificación completa."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(./init.sh)",
      "Bash(python3 -m unittest*)",
      "Bash(python3 -m src.cli*)",
      "Bash(bash init.sh *)",
      "Bash(uv run ruff check . 2>&1)",
      "Bash(uv run ruff format --check . 2>&1)",
      "Bash(python -m pytest *)",
      "Bash(python -m venv .venv_test *)",
      "Write(progress/*)",
      "Write(src/saas_radar/**)",
      "Write(tests/**)",
      "Edit(feature_list.json)",
      "Edit(progress/*)",
      "Edit(src/saas_radar/**)",
      "Edit(tests/**)",
      "Edit(.claude/settings.json)",
      "Edit(CLAUDE.md)",
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git push *)",
      "Bash(git status)",
      "Bash(git diff --stat *)"
    ]
  }
}
